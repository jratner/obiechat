<div class="topicPreviewsContainer">
  {{#each topics}}
    <div class="topicPreview">
      A TOPIC PREVIEW
      <div class="topicName">
        {{ name }}
      </div>
      {{#if owner}}
        <div class="owner">
          {{ owner }}
        </div>
      {{/if}}
      <div class="lastMessage">
        Not doing this yet.
      </div>
      <div class="type">
        {{ type }}
      </div>
    </div>
  {{/each}}
  <div class="topicViewArea">
    A TOPIC
  </div>
</div>